<Window x:Class="Market.UI.Views.PesoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Informar peso" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="20">
        <TextBlock x:Name="TxtProduto" FontSize="18" FontWeight="Bold" TextWrapping="Wrap"/>
        <TextBlock x:Name="TxtPreco" Foreground="#4C566A" Margin="0,4,0,0"/>

        <TextBlock Text="Peso (kg)" Margin="0,16,0,4" FontSize="13" Foreground="#4C566A"/>
        <TextBox x:Name="TxtPeso" Height="42" FontSize="22" VerticalContentAlignment="Center"
                 MaxLength="8" AutomationProperties.AutomationId="TxtPeso"
                 PreviewTextInput="Decimal_PreviewTextInput"
                 DataObject.Pasting="Decimal_Pasting"
                 TextChanged="TxtPeso_TextChanged" KeyDown="TxtPeso_KeyDown"/>
        <TextBlock Text="Ex.: 0,750 para 750 gramas" Foreground="#8A94A6" FontSize="11" Margin="0,4,0,0"/>

        <Border x:Name="PainelTotal" Background="#E8F5E9" CornerRadius="6" Padding="14" Margin="0,16,0,0">
            <Grid>
                <TextBlock Text="TOTAL" Foreground="#2E7D32" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtTotal" AutomationProperties.AutomationId="TxtTotalPeso"
                           Text="R$ 0,00" Foreground="#2E7D32" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancelar" Width="100" Height="36" Margin="0,0,10,0"
                    Click="BtnCancelar_Click" IsCancel="True"/>
            <Button x:Name="BtnConfirmar" Content="Confirmar" Width="120" Height="36"
                    Click="BtnConfirmar_Click" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
